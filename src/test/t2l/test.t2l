import java;
import system;

func test "hmm" {
    return hmm + 3;
}

x = java.asJava(test);
params = [3].iter(system.getExecutor());
print(x.run(null, params, system.getExecutor()));

for i in [1,2,3] {
    print(i);
}